+++
title = "如何部署 Reality协议 服务端负载均衡"
date = 2026-05-26T01:14:52+08:00
draft = false
description = "最近更新了一个比较厉害的功能，嘿嘿 就是！可以在服务端对 Xray 的 Reality协议 进行负载均衡，哼哼 服务器示意图如下👇 一级服务器（主服务器）如何搭建 首先你要运行脚本："
slug = "bushu-reality-balance"
featureimage = "/images/微信图片_20250902162634_61_354.jpg"
categories = ["网络技术"]
tags = ["Xray", "Reality", "负载均衡", "代理"]
+++



最近更新了一个比较厉害的功能嘿嘿，就是，可以在服务端对 Xray 的 Reality协议 进行负载均衡，哼哼

![](/images/微信图片_20250902162634_61_354.jpg)大概访问情况如上

## 一级服务器（主服务器）如何搭建

首先你要运行脚本：[** _Xray_bash_onekey_**](https://github.com/hello-yunshu/Xray_bash_onekey) ，并且确保版本在2.7.0及以上

然后，在主服务器上，正常运行安装 Xray (Reality+ws/gRPC+Nginx)

运行中，当脚本问你：  
• 要不要开「**负载均衡** 」**--- >** 必须开！  
• 要不要开「**Nginx** 」**--- >** 也必须开！（因为负载均衡就靠它诶）

脚本在运行是会提问的，如下图：

![](/images/wechat_2025-09-02_163352_850.png)

在按照流程搭建时，必须要记下的四个重要的值：

  1. **UUID 映射字符串（UUID）**

  2. **Reality 需要的Target域名**

  3. **privateKey**

  4. **shortIds**

上面👆的值非常重要，每个都要记下来，知道不？

忘了也别慌，脚本第 17 项能随时查～

## 二级服务器（次服务器）如何搭建

同样是运行脚本：[** _Xray_bash_onekey_**](https://github.com/hello-yunshu/Xray_bash_onekey) ，并且确保版本在2.7.0及以上

然后，在次服务器上，还是正常安装 Xray (Reality+ws/gRPC+Nginx)

安装时：  
• 自定义端口**--- > **建议用高位端口，比如 2xxxx  
• 自定义**UUID / Target / privateKey / shortIds --- > **必须和主服务器一模一样！

![](/images/wechat_2025-09-02_165730_864.png)

如上图，请一一自定义之前加下的参数，保持与主服务器一致

在选择是否需要开启 Nginx 的时候，可以选择不开启，也可以开启。但是建议如果只用做次服务器**不要开启**

安装完后，记下次服务器的**IP（建议内网 IP）** 和**端口号**

## 一级服务器如何连接二级服务器

回到一个服务器里，在脚本里选择 _10. 变更 Nginx 负载均衡配置_ ，然后会有如下提示：

![wechat_2025-09-02_170104_729.png](/images/wechat_2025-09-02_170104_729.png)

根据要求，创建一个新的 realityServers 文件，创建过程会提示你输入：

  1. **主机 (host) --- >**次服务器的**IP**

  2. **端口** **--- > **次服务器的**端口号**

  3. **权重** **--- > **默认就行，想偏心谁就调大点

输入完后，就自动连接上啦！！

可以在次服务器运行脚本第15个选项，能看到流量就说明没问题啦～✌️

## 客户端怎么使用（优势）

客户端**只需要使用主服务器** 生成的配置即可，**次服务器不要使用**

其实，完全可以搭建多个服务器，在客户端实现负载均衡。但是呢，分开请求可能会受到较慢的服务器拖累

所以！过去想在客户端做多 IP 负载均衡，配置一大坨还容易踩雷。现在只需要连主服务器，就等于偷偷访问了后面一群小弟，又快又稳！

而且呢，还能把次级 IP 藏起来，不怕被 ban，超安心～😊

## 注意事项

**强烈建议** ：主服务器 + 所有次服务器放在同一个区域、同一个内网，理由如下：[关于脚本的负载均衡功能 - 云云舒](https://hey.run/archives/1715234393850)

记得在 VPS 控制台放行次服务器的端口，但只对内网放行就行t

次服务器没有 Nginx 挡子弹，千万别让它乱跑流量，只当负载均衡小透明！

安装完后，脚本会有额外提示，如图：

![](/images/wechat_2025-09-02_174135_399.png)

看到 +Balance 就意味是负载均衡的服务器，这样就不会搞错啦

一些杂项：

  * 建议都开启 fail2ban

  * 建议都开启 TCP加速

"]},"target":{"position":0,"lines":["

最近更新了一个比较厉害的功能，嘿嘿

就是！可以在服务端对 Xray 的 Reality协议 进行负载均衡，哼哼

服务器示意图如下👇

![](/images/微信图片_20250902162634_61_354.jpg)

## 一级服务器（主服务器）如何搭建

首先你要运行脚本：[** _Xray_bash_onekey_**](https://github.com/hello-yunshu/Xray_bash_onekey) ，并且确保版本在 **2.7.0** 及以上

然后，在主服务器上，正常运行安装 Xray (Reality+ws/gRPC+Nginx)

运行中，当脚本问你：  
• 要不要开「**负载均衡** 」**--- >** 必须开！  
• 要不要开「**Nginx** 」**--- >** 也必须开！（因为负载均衡就靠它诶）

脚本在运行是会提问的，如下图：

![](/images/wechat_2025-09-02_163352_850.png)

在按照流程搭建时，必须要记下的四个重要的值：

  1. **UUID 映射字符串（UUID）**

  2. **Target域名、serverNames（如果你自定义了）**

  3. **privateKey**

  4. **shortIds**

上面👆的值非常重要，每个都要记下来，知道不？

忘了也别慌，脚本第 17 项能随时查～

## 二级服务器（次服务器）如何搭建

同样是运行脚本：[** _Xray_bash_onekey_**](https://github.com/hello-yunshu/Xray_bash_onekey) ，并且确保版本在 **2.7.0** 及以上

然后，在次服务器上，还是正常安装 Xray (Reality+ws/gRPC+Nginx)

在脚本运行中：  
• 自定义端口**--- > **建议用高位端口，比如 2xxxx  
• 自定义**UUID / Target / privateKey / shortIds --- > **必须和主服务器一模一样！

![](/images/wechat_2025-09-02_165730_864.png)

如上图，请输入之前记下的参数，保持与每个都与主服务器一致

当脚本问你：  
• 要不要开「**负载均衡** 」**--- >** 必须开！  
• 要不要开「**Nginx** 」**--- >** 建议不要开

在选择是否需要开启 Nginx 的时，如果只用做次服务器，建议**不要开启**

安装完后，记下次服务器的**IP（建议内网 IP）** 和**端口号**

## 一级服务器如何连接二级服务器

回到主服务器（一级服务器）里，在脚本里选择 _10. 变更 Nginx 负载均衡配置_ ，然后会有如下提示：

![wechat_2025-09-02_170104_729.png](/images/wechat_2025-09-02_170104_729.png)

根据要求，创建一个新的 realityServers 文件，创建过程会提示你输入：

  1. **主机 (host) --- > **次服务器的**IP（建议内网）**

  2. **端口** **--- > **次服务器的**端口号**

  3. **权重** **--- >** 默认就行，想偏心谁就调大点

输入完后，就自动连接上啦！！

可以在次服务器运行脚本第15个选项，能看到流量就说明没问题啦～✌️

## 客户端怎么使用（优势）

客户端**只需要使用主服务器** 生成的配置即可，**次服务器的不要使用**

其实，完全可以搭建多个服务器，在客户端实现负载均衡。但是呢，分开请求可能会受到较慢的服务器拖累

所以！

过去想在客户端做多 IP 负载均衡，配置一大坨还容易踩雷。现在只需要连主服务器，就等于偷偷访问了后面一群小弟，又快又稳！

而且呢，还能把次级 IP 藏起来，不怕被 ban，超安心～😊

## 注意事项

**强烈建议** ：

  * 主服务器 + 所有次服务器放在同一个区域、同一个内网，理由如下：[关于脚本的负载均衡功能 - 云云舒](https://hey.run/archives/1715234393850)

  * **主服务器** 选择 **延迟低 + 带宽高** 的喔，毕竟客户端连接次服务器全靠它

记得在 VPS 控制台放行次服务器的端口，但只对内网放行就行

次服务器没有 Nginx 挡子弹，**千万别让它乱跑流量** ，只当负载均衡小透明！

安装完后，脚本会有额外提示，如图：

![](/images/wechat_2025-09-02_174135_399.png)

看到 +Balance 就意味是负载均衡的服务器，这样就不会搞错啦

一些杂项：

  * 建议都开启 fail2ban

  * 建议都开启 TCP加速


